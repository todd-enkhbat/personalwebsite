export function SiteFooter() {
  return (
    <footer className="mt-16 border-t border-black/10 pt-6 text-sm text-muted">
      <p>© {new Date().getFullYear()} Todd Enkhbat — exploring space, philosophy, and finance.</p>
    </footer>
  );
}

